# gitrex

`gitrex` is a terminal-first git manager written in Rust.

It defaults to a TUI in interactive terminals and falls back to CLI behavior for scripting and pipes.

Repository conventions and context live in [context.md](./context.md) and [context-format.md](./context-format.md).
